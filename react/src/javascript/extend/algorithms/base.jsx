export default class AlgorithmBase {
	constructor (props) {
		super(props);
	}

	static validate (data) {
		console.log('you must implement this template function name "calc" inherits from the AlgorithmBase class first');
	}
}